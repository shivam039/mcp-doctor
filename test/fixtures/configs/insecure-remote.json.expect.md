Expect: against a live server at this URL, `security.untrusted-remote` reports one `warning` diagnostic ("connects over unencrypted http://") carrying a `suggestedFix.patch` of `{ serverName: "insecure-remote", set: { url: "https://api.example.com/mcp" } }`. `mcp-doctor fix test/fixtures/configs/insecure-remote.json` shows that diff, and on confirmation rewrites the `url` to `https://` and writes `insecure-remote.json.bak` with the original content first.
