Expect: `loadConfig()` accepts this with zero errors (`headers` is a valid optional field on http/sse per CONTRACT.md). As of this writing Codex's protocol layer (`src/protocol/`) has not landed (still just `.gitkeep`), so header passthrough on the real HTTP request is unverified — re-check this fixture once `src/protocol/` implements the http transport to confirm the `Authorization` header is actually forwarded, not just parsed.
